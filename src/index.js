import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <div className = "app">
      <h1 className = "titulo">
        Portfolio
      </h1>
    </div>
  </React.StrictMode>,
  document.getElementById('root')
);

